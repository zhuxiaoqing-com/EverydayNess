Node之间互相连没关系，当然应该还要加一个字段标识是否需要连接这样;
比如战斗服务器需要4台，一台里面n个service 比如10个，那这个时候是不是就不直连比较好？
好像都直连也没事，一般应该只会网关一台服务器;玩家logic一台服务器或者玩家公共lobby也在一台服务器;然后就是Map一台服务器;
所以最多也就4个或者5个Node, 5x5是25条链接 问题不大;

如果是跨服玩法的话 那就需要单独连接节点了，不能直接连接;
比如需要一个路由节点，路由到不同的跨服地图，如果是单个跨服玩法的话，我感觉可以全部直连，也可以全部走lobby然后再转发过去，这个可以后续再看


我感觉你这实现的Location 完全不能用，ET的话 是如果是用actorId(应该就相当于我们的CallPoint)的话就直接发送，如果使用entityId的话就需要找entityId所在的服务器代表的CallPoint;
然后服务器这边应该也有返回报错，没有其callPoint,ET的话MailBoxComponent 我这个项目的话是没有这个的 你帮我分析下 我应该怎么弄

    感觉确实应该来一个MailBox,默认比如像是公会的话 自己就是一个MailBox,像是玩家服务器，地图服务器，应该自己有一个MailBox,然后玩家也应该有一个MailBox;
    我需要先看下ET是怎么做这方面的;怎么知道某些消息玩家需要MailBox,某些又不需要，难道是代码里写死的吗？
    我看应该是写死的;

    现在是如果我要发给玩家 我是不是要调用一个专门的方法，然后吧玩家id放入呢;然后对面,查询玩家id是否在其service里面,如果不在就返回没知道actor;
    所以感觉service里要有一个判断玩家是否在在线列表里的，如果这个service不需要判断的话，那调用这个也没事 不需要返回找不到;
    地图service 网关service 感觉应该都是需要; 地图的话是要给指定的玩家发送消息,网关的话,是要将消息发送给客户端;
    那是不是rpc的调用内部，也不用扩展到外面，就内部是不是应该有一个errorId进行传递呢,然后就框架内部使用，也不开放给业务;
       感觉可以弄一个Context;然后如果有这个就传入errorID，如果没有就不传入(这感觉很难实现啊)




Cause: org.evd.game.StageService.HaHaHaActor 不是 owner service，只能使用 RpcRoute.LOCATION ;
我想的是RPC里标识什么就写什么啊，感觉生成的XXXProxy 可以直接都用static，
然后每次固定传入actorId(最好是传入一个long 这样吧) 或者CallPoint; 感觉要new 会有很多new方法啊;


你的locationService 不对啊，怎么东西都是自己手动生成的，改成Rpc生成啊;

conService 接入netty，netty和@ClientCmd注解生成的方法接入,netty的接口要写的通用;

我现在这所有的东西都抽成协程来运行 连tick也变成了协程 但是好像又只能协程，因为万一 tick里要进行rpc调用呢，但是进行了rpc调用的话 tick不是就很奇怪，
可能后面10个tick都运行结束了 当前tick才开始 et里是怎么弄的


我想将mdbPlayerThread接入，或者说可以自己写java类，然后加注解，但是怎么将dirty写入呢？
	只允许写字段 不运行加方法？然后用APT生成方法标注apt,那集合呢？可以模拟mdbPlayerThread 生成对应的方法;
	到时候看看吧
	数据库类生成好了，应该有个方法，是否是要将某一个key的全部load出来？
	那像是工会什么的呢，好友什么的呢？
	玩家数据可以全部加载,那就需要一个方法获取service所有的表,

增加DBService;
Table获取从数据库查询使用callWait，
	get 使用callWait;
	checkpoint? 直接call checkPoint，根据dbService+key分组 一个一个更新吧 就不批量更新了;可能可以DBService 批量;
	loadMemory呢？直接遍历调用get;
	flush 根据tableName+key 全部打包好给dbService提供的接口？
	clearCache不用说，就单个删除就行了;但是删除以后需要返回这个怎么弄呢？

我决定了先弄第一版出来，不管什么version，就先可以批量和单个，然后直接全部用replace, remove；这两个;
		只管pb json mysql;三种;
		replace remove  get replacebatch 和 removeReplaceBatch getbatch
		然后定义发送中间类 和接受中间类;
		手写 TTable类，和 Mdb全局数据库类，
		db类的话再生成继承TTable类，里面实现add remove get三个方法;
		Mdb数据库类 启动的时候将所有的数据库类的TTable都加载，然后每个TTable都有一个hashMap<int,int>
		也就是自己的内存缓存，然后其原来的查询数据库 存储数据库 就给翻译成放DBService里面;
	get：
	delete
		json   getkey()
		pb getkey()
		mysql  getkey()
	replace
		json   Object[];
		pb Object[]
		mysql  Object[]

	 现在有三种方式
		DB数据类放common 直接序列化过去使用SerializeClass;
		DB数据类放common 序列化过去使用专用序列化,根据mysql  pb json 变成对印的数据结构 比如F:\gittttt\EverydayNess\core\runtime\src\main\java\org\evd\game\runtime\DbEntity\serialize\MysqlReq.java;
		我感觉理想的就是像Mdb那样，一个DB类，一个Table类 生成sql 或者其他，然后New对象，将数据序列化进DB类;
		但是这里就没法做隔离DB类，除非而DB类 我是确定要隔离的;加一个描述类生成sql或者其他，
		然后弄个Table类进行序列化DB类转成专门的Mysql请求类 反序列化mysql请求类序列化成DB类; 然后就是基础的 get replace remove;
		描述类的话：create update get remove sql; 以及映射扫MysqlReturn类 这个MysqlReturn 映射到底是放Mysql访问类，还是说吧PreparedStatement和ResultSet传入进来操作

	get 需要访问固定结构 加+固定序列化，这个可以做适配层吗;
	checkpoint;
	flush;
	loamemory;
	clearExprieCache;
	本质上应该就是序列化 和反序列化的数据不一致;
	序列化和反序列化用自己生成的Table来操作吗;
	感觉需要一个序列化Table;这个table怎么弄，接具体的序列化操作吗,
	除了序列化 还需要选dbService接口 难搞
	一个add remove Table;
	我悟了 其实就是MDB的 StorageMysql; 我只需要把对象传入进去，然后调用DB专门的序列化方法就行了;
	那其实最好的方式DB序列化 还是放DB类里最好; 不然的话 写起来很麻烦;
	还有一种就是 其实不需要专门的适配类，只需要序列化对象 和反序列化对象弄成单独的类，然后处理;

	db数据类单个DBReq的可以直接用方法将其抽象出来，如果是batch形式的呢？其实也行，使用static就行了;
	然后传入多个相同的对象，在里面构建; 没毛病！





切换地图流程
1. Client 请求切图
   Client -> Gate -> MapA

2. MapA 本地锁玩家
   player.state = TRANSFERING_OUT
   transferId = newId

3. MapA 请求 Location 开始迁移
   Location.BeginTransfer(roleId, MapA, transferId)

4. MapA 生成 TransferSnapshot

5. MapA 保存玩家永久数据到 DB
   Save PlayerData
   Save lastMapId / lastPosition

6. MapA 发送快照给 MapB
   MapB.TransferIn(snapshot)

7. MapB 创建玩家
   player.state = TRANSFERING_IN
   初始化 Unit / AOI / 坐标 / Buff / CD

8. MapB 完成 Location 迁移
   Location.CompleteTransfer(roleId, MapA, MapB, transferId)

9. MapB 通知 Gate 改路由
   Gate.ChangeRoute(roleId, MapB)

10. MapB 激活玩家
   player.state = NORMAL
   下发 TransferComplete / 场景信息

11. MapB 通知 MapA 删除旧玩家
   MapA.RemovePlayer(roleId, transferId)

传输的时候还有一些内存数据也需要传输的 别忘了！！！


这些弄好了 是不是就可以写例子了，现在已有 conService LocationService StageService, 再来个GuildService，感觉就已经可以写代码了，然后
在StageService里进行场景代码写入,  来一个ActivityService

这个架构要是弄跨服怎么弄呢？比如地图服务器 有本服地图服务器和跨服地图服务器; 地图服务器里加个标识吗;


2184315555723333663
2184621224083363891


isolating / aggregating filer  将APT 修改为这个形式的; RPC的话common的数据特殊处理，使用File写入，每次先删除 再写入;

  用isolating / aggregating filer的概念生成; 我看ClientCmdProcessor 生成的都是聚合类把

将DB改成version格式的，因为batch可以每一条消息都返回;所以可以确定返回是否更新成功;
	不对checkpont 应该不需要等返回的; 或者也需要等返回; DB缺一个聚合RPC，把多个DBReq收集起来一起执行;
	省的一个RPC结束才能进行下一个 显得卡;

Processor的aggregating 类型的 最后将其分片一个源类就是一个生成类，然后启动的时候收集起来，防止后期协议多了太卡




其实client 也需要用MailBox的，而且应该是要默认就有MailBox的吧;当然也不是，比如客户端请求工会列表，这个时候应该就不用MailBox;
	客户端请求玩家数据，就要跟着玩家走;MailType 有点奇怪，现在在哪里都能标识，其实感觉应该只能在Service上标识，可以有哪几种MailBox;
	这个怎么弄;

其实对应到传统的服务器就是，我在gw里就已经缓存了在哪个logic 在哪个map,然后看协议要往哪里转发- -;
还有一个就是 是否一个service要对应多个MailType呢

目前定义的服务器
   GateService 分片;管理客户端连接、心跳、断线检测、消息转发
   OnlineService;分片，管理玩家在线状态、登录流程、sessionId、当前所在玩法/场景
   LocationService;可以按 playerId % N 分片;管理 playerId/entityId -> actor地址/sceneId/instanceId
   SceneService; 动态负载; 管理地图、场景、战斗、玩家实体
   PlayerService; 动态负载; 玩家单人玩法服务器;
   MultiPlayService; 单点; 管理公共玩法、活动、副本入口、匹配、玩法实例创建
   GuildService; 分片;
   FriendService;单点; 管理好友、黑名单、社交关系
   DBService; 分片; 负责持久化

    这里所有的分片都有一个问题，就是要是分片要扩容或者缩容怎么办;
    按以前的服务器思路，根本没有缩容扩容这一说，除了场景服务器;和玩家服务器;这两服务器都是跟着玩家绑定的;可以负载均衡;
        缩容就踢人;玩家服务器就踢下线,场景服务器就回到主城;
        当然这个需要一个统一的服务器，这个服务器也可以做扩容，那就得吧数据放redis里了

     工会分片不管，直接固定分片，挂了重启就好了;


其实所有的都可以弄分片，只需要两个服务器，一个location服务器，一个是分配服务器，这两个服务器怎么支持集群，其他都不是问题，工会只需要和玩家一样也有登录的概念，就可以分片了，玩家好友就更不用说了，
也可以，或者就是用过期的方式，就是每30分钟重新分配一次，像是好友服务器的的话，发送方发送过来，怎么知道过期了呢？直接重新分配的时候给所有好友服务器一个新的%，然后先锁location;
分配完了以后解锁location; 但是location怎么弄集群呢 应该就不行了
其实所有的分片都需要两个东西，一个是负载均衡分配;一个是location服务器，在哪个服务器;还有一个就是，远程服务器怎么校验消息是否合法(是否属于该服务器);
    假设全平台好友服务器,这种直接用web的方式写都行啊额，感觉只有那种强状态的才需要用我上面的方式; web的方式其实也很麻烦，需要redis mysql;
现在就讨论我这location和分配服务器应该怎么弄;
    假设Guild服务器，一开始是4台，连接上分配服务器后，分配服务器给1-2-3-4个id和总数;然后放location保存,1-2-3-4分别代表哪几个服务器的链接;
    每台guild服务器，在收到guild操作以后，进行用分配的id和总数，进行hash %计算，不满足的就return 返回找不到actor;
    然后当新增guild服务器的时候，先锁location;不行这样锁的粒度实在太大了;
       或者应该用guildId来操作？这个没问题;如果没有映射的就走负载均衡服务器负载一个公会服务器;

游戏服尽量不要出现全平台redis和全平台mysql;最好用一个服务器包一下，比如db服务器;
实际一点的，感觉一般就是匹配是全区全服的;还有排行榜是全区全服的;排行榜没啥好说的,最多只能按榜分;而且一般也不需要动态;
    匹配的话，支持挂了，支持动态增减;匹配是有状态的;有个路由服，所有的玩家先匹配都要去路由服进行路由;然后分配匹配服，
        匹配服挂了的话，就那部分玩家全部退出匹配,没问题;假设匹配逻辑是，匹配成功，给场景服务器发送创建场景，然后再让玩家进入这些地图，如果这个时候玩家又在匹配状态了，那就不能进;这个看业务逻辑锊;
        其实这也相当于是缩容了，如果还要无感的缩容的话，就匹配服弄一个状态，当前匹配服已经是准备关闭中，就不给新的玩家安排匹配服了，然后固定比如30秒后缩容，因为有可能有玩家永远也匹配不到人;缩容状态下，就不给匹配机器人什么的(如果匹配有这个规则的话);
        扩容呢，有路由服，其实就是分配给路由就行了 额;
        这里有个问题是路由是单点的，有点问题,路由其实可以弄redis 弄成分布式的，然后加分布式锁就好了;或者其实不需要分布式锁，就是查询匹配服的负载;
           其实这路由服好像就和DB服一样，用来削峰（共用redis连接2）用的;
        说这么多就这几种
        1.匹配服单点/数据放redis可恢复;/或者主从,消息同步到从以后再返回;
        2.匹配服分片;
        3.匹配服+路由服，匹配服直连游戏服,路由服分配匹配服,同时接受多个匹配服的负载均衡,/也可以不要路由服，数据直接上报redis，这样的话就需要一个全服redis;
        5.匹配服+转发服+路由服/或者匹配服+转发路由服(消息通过转发服，这样匹配服扩展无感 不像战斗服，匹配转发服的量并不会特别频繁;)


服务器大致确认好了，就这样吧;
然后还是MailBox怎么弄;我可以按两种，一个是按指定的key来分配，一个是就是发给执行服务器的操作来;
    但是是不是有些key要同步执行呢，这个东西怎么弄，比如player线程，玩家操作正常是要排队执行的;
只有玩家操作需要order,其他全部unorder; 特别需要警惕玩家交易，应该放在公共服务器;进行交换;

感觉一个Service 还是需要多个MailBox的;
    比如场景Service,地图玩家Box，场景box,服务器本身box;为什么要分这么多，主要是因为需要找玩家，和找场景，我要给场景发协议，不管玩家当前在不在场景里，我要给玩家发协议，
    不管玩家在哪个场景;Service上标识这个Service可以用哪几种MailBox;感觉应该是要在 RpcActorType 里标识，只能支持哪个Service吧，毕竟一个Service只能一个RpcActorType;
    但是RpcActorType又没法引用具体的Service.class,所以其实可以吧Server弄个Type出来，CallPoint也就不需要写Service名字了;

MailBox+ActorId
    ActorId直接指向MailBox不行吗，为什么要加一个MailBoxInstanceId呢;
    因为切换地图以后，正常会删除MailBox+新增一个MailBox,如果没有MailBoxInstanceId 消息有可能在旧的MailBoxInstanceId处理消息的时候,
    直接运行消息，破坏协程锁的线性执行;

    那我是不是不用协程锁？直接用个队列不行吗，也不行，这个时候旧的队列处理完，引用新的队列的数据也会报错;
    但是发送消息的时候是不是不需要知道MailBox呢?在消息处理的时候在知道MailBox不就行了吗？

    RPC怎么投放MailBox已经知道了;看了ET的Unity/Assets/Scripts/Hotfix/Server/Demo/Gate/NetComponentOnReadInvoker_Gate.cs:13
    也知道了Cmd是怎么投放的了，就和RPC一样，然后几种Actor类型，分别代码写死找对应的服务器;

跨服模型;
需要一个serverId;需要一个服务器id;
     然后连接的话，因为默认如果该Node在配置里不存在会自动将其加入到内存里;
     现在就是这么调用的问题了;
     比如global服务器按传统的逻辑，全部和Lobby相连;
     global_battle除外也和logic相连;

    然后之前battle数据存到了lobby,所以lobby连接上battle以后需要将数据发送到battle让其恢复地图数据;
    service里应该怎么做

    然后我怎么让本服访问地图，和跨服访问地图一样简单呢?
    之前传统的话 是用同一套代码，这里的话怎么弄;

    按之前的lobby 直接lobby名字发送，按logic的话，就直接获取logicId发送;
    按battle的话之前是根据 本服还是跨服地图，发送到不同的服务器;然后也有服务器id的;
    这里有一个问题就是Service怎么复用，这是一个很大的问题;解决了+ServiceType

    location架构迎来了大动荡;加了跨服玩法以后Location就失效了;比如我在本服,给玩家发数据,使用ActorType.Gate,id玩家id,
    在跨服呢?location是单服的东西啊,
    想了一个法子，location也有跨服的,然后id全部使用idFactory生成带平台id和服务器id,然后跨服的服务器id直接固定死一个数值就好了;可以是0?
    然后本地和跨服都直接根据id来进行查看是访问Location,还是GlobalLocation;
    场景还有一个问题，就是场景按之前的项目来说，其实用mapType+map创建方式来创建一个字符串更好用;比如这样系统副本也可以创建多个,想进哪个还不需要特殊处理;
    那actorId是不是应该用String替换?还是根据type获取long还是String的;
    	public String getKey(int dungeonId, long groupId, int serverGroupId, int areaId) {
    		return keys.getKey(BattleMonitorServer.getLobbyId(), dungeonId, groupId);
    	}
    	想这样子的,系统也可以用groupId来区分
Id主要是用来区分，global服务器想获取玩家的某个数据在在哪里的，可以通过id获取玩家的逻辑服务器，然后用来发协议;
玩家连接上服务器的时候，必须将玩家当前的serverId和以前的合服id一起发送过去;
其实这里我不是很明白，跨服的话不是肯定要玩家的服务器Id的吗，既然这样直接通过玩家服务器id访问玩家所在的location服务器，然后获取玩家网关不就行了吗 额
    哦哦 之前我这里的locationService不止记录玩家在哪，还记录某个工会在哪个服务器，某个场景在哪个服务器这样的;

考虑业务逻辑怎么写，要不要用接口，要什么东西直接继承一下接口，RPC要不要先定义接口;
现在已经在写登录逻辑了，感觉简单一点的话，可以直接继承接口，然后APT生成，或者启动收集，调用的时候是吧对象缓存复用呢，还是说每次都new;


db服务器，我看了阿拉德之怒的RecordServer，发现变化了就往RecordServer推送，默认是mysql字段含义，变化了同步一个字段，
    然后RecordServer默认5秒自动落库一次;RecordServer里默认有个内存缓存;

现在感觉有
1.去掉DBService 本地直接跑缓存+数据库 + R2DBC
2.DBService 还是只用于访问数据库，只要一个就好了  + R2DBC
3.DBService 也有内存缓存，也是定期保存, 可能游戏服务器里保存可以快一点？然后不怕脏数据，其实也怕，但是可以玩家离线就直接把数据删了,
决定了 DBService 就弄一个就好了，然后消息过来，先直接缓存起来，然后再定时存;
大量的查询消息 顶不顶得住，感觉应该也还行 就登录的时候大量查询下;而且本身就是用异步R2DBC存储，好处就是定时存，定时删会很快就存好;
DBService 收到消息以后缓存，然后直接返回 内部异步存储，还是说让业务逻辑等Mysql访问结束呢？ 哪个好
     感觉缓存一下好，这样的话 只要消息发送过来不用担心mysql 挂了 只要dbService启动了就没问题，可以把mysql从链路排除出去;

停服流程 添加一个AdminService 然后由这个服务器来触发关服流程，根据ServiceType里定义的关服顺序来进行关服，
    关服的时候服务器内部的连接先不关，而是先设置状态，然后Service在某些状态的时候不做某些事情，不然httpService会没有返回的;
    必须要有返回才能确定关服成功还是失败;
    GW 网关将对外的端口关闭，直接close关闭即可;然后所有的玩家就会自动执行下线流程，没有执行完也没事;
    SCENE 直接关闭，如果某些Map需要保存数据，那就直接在原地保存就好,关闭之前，先设置状态，保证不会再产生新的地图心跳，
            然后将所有的协程相关的容器里的数据全部清空,比如协程队列，timer队列，wait队列, 然后再调用每个地图的关服回调，在回调里可以保存数据，但是不能调用waitCall之类的方法;
    LB    关闭 保证玩家下线，保证MDB结束
    LG      关闭 保证玩家下线，保证MDB结束
    LOC 直接关闭
    DB  保存内存数据 持久化到数据库

   然后httpService再统一调用关服，直接退出虚拟机即可,其实可以最后让服务器取消注册service就行，可以一直sleep等待，这样的话就不用发两次了，很好;

   在这里是不是需要先把ZMQ都替换成netty在弄呢?先想清楚关服流程吧 先不替换了，然后还要考虑kill的情况应该怎么弄;尽量保证完整,只能说;

    决定了可以在onClose里退出，也可以在一个rpc协议里退出，或者在stop里退出，在stop里退出吧，钩子的话就在stop里加一个退出的钩子

    很乱啊感觉发送消息;
    正确的模型应该是，消息应该和超时统一，不能消息发出去，但是又触发断线流程，但是消息对方又收到了;
    service里发送先放到buffer里面，然后每帧传输给node，但是消息断链又是直接抛给service;
    这里就会有service断链了，将其wait过期了，但是Buffer里又将消息输出出去了;
    感觉确实ET那种的将消息放在channel里模型要正确很多，我这个项目的代码感觉用channel模型又不太可能呀;
    看了ET实现，是直接等超时结束额;

    这个先写一版，然后切个分支，后续决定弄服务注册+两条RPC连接了，RPC超时了就业务层重发+幂等性检测，感觉这个要更先进一点;

    连接暂时还是这样，node服务器，remoteNode客户端， service 缓存一个类似，channel的数据，然后将其缓存起来？
    不行，最好还是一个service一个连接，这个其实多几个连接我感觉无所谓啊，或者其实连接还是Node级别的，然后抽象一个service级别的连接,
    主要是怕跨服服务器连接太多;这个一个node其实就要三条连接，两条RPC，一条注册中心,抽象一个session的概念，如果不存在就创建session，然后将数据缓存在上面。
    session 不存在就进行创建连接，如果channel已经存在就关联，如果没有就进行连接，如果正在连接中就不处理，如果有连接 但是链接断开了，就抛弃;
    ET是如果是同进程的话就直接将协议发送给并发队列，然后每帧取1000出来处理，如果是发给跨服的话，是弄一个netFiber,由他发送，
        所以他获取到ip端口了，也没事会有netFiber进行连接的;
        内部通讯有安全队列，发送给NetFiber也有安全队列，然后NetFiber发送给session，至于连接的话，是在actorId里会自动带的，连接不存在就自动创建;
        按照ET的架构，我感觉我这个Node这里应该直接缓存service发送的call吧，然后再Node里最终要发送的时候，再用缓冲区发送,但是我缓存了这个call 会不会影响GC的，
        将channel里放一个并发队列(或者包装一下channel 看下怎么好)，然后将call放到channel里面，过期了，先把channel=null,然后将并发队列里数据清空，也有问题啊，除非抛到同一个队列执行，
        假设抛到node队列里面，然后node队列每帧取数据，进行发送，其实ET也会有这边超时了 消息发不出的问题？不会的除非真的超时了;
        所以这里抛进去，然后node取出来分发发送，然后如果channel不存在就进行超时处理，如果又连接上了，就继续发送;不存在或者发送不了就回调给service.
        如果掉线，回调给node是线性的，Node将指定的channel上的消息超时，那些在缓存里还没取出来的就先不管了,后续直接超时，应该也不怕马上又连接上了，基本不存在，
        真的出现了就让继续发吧

    然后就是注册中心了，看看ET怎么写的;