地图状态：
    发起进入地图;
    进入地图完成; 先简单弄这两个吧

PlayerService发起进入地图，
    设置进入地图状态; 检测当前其他状态是否满足;
    查看上一个地图id,发往SceneManagerService进行退出地图;
    SceneManagerService查看地图在哪个Stage，这个应该不需要，
    PlayerSerivce知道玩家在哪的，直接访问即可;
    查看上一个地图，直接给上一个地图发送退出地图,如果不在上一个地图，就直接返回，
    在的话，就调用退出地图，然后里面会有退出地图的协议发送，这边退出地图完成，继续回来执行进入地图逻辑,
    访问SceneMangerService;查看对应地图id所对应的地图，没有的话，或者根据策略来生成在SceneManagerService的地图Id;
    然后负载均衡一个Stage进行创建地图,到了Stage创建地图，返回SceneMangerService创建成功;
    然后将等待在SceneManagerService的玩家进入地图;走完进入地图逻辑 返回PlayerService


    Player 进入地图->先退出上一个地图->访问SceneManager获取地图数据->如果地图没创建就进行创建->
    创建或者查找到了就发送消息给Player,->这里查看是否已经离线,离线就直接return;
     记录即将进入的地图唯一id 然后进入地图(这个最好单独一个方法，后面匹配成功就是直接调用这个方法进地图就好了)->
     发送给Stage进入地图;Stage保存玩家数据标识玩家即将进入某个地图;
       ->返回PlayerService，

                这个时候玩家要进入另外的地图，因为在进入地图状态中，所以禁止进入地图;
                        实际上是进入A地图时，然后进入B地图失败;但是也可以理解为,进入B地图成功以后,又去了A地图;所以这个禁止地图不是什么问题;

    如果这个时候离线了;就会触发离开地图,这个时候离开地图，直接查看记录的即将进入的地图id,地图callPoint等数据，直接发往那个地图;
        要么是在准备进入地图状态;要么是进入地图状态;准备进入地图状态，就直接删除状态，已经进入地图就运行退出地图;


最新的进入地图逻辑;
    显示场景管理服;进去判断场景是否存在，不存在就创建，然后玩家队列挂在场景管理服;
    然后就是场景服 检测是否可以进入场景，可以的话就存预进入队列; 然后去退出上一个地图,然后退出完了以后,等客户端进入地图加载完毕，再进入这个地图;
    完美;

退出地图呢 只在离线时操作 如果是在完全进入地图的情况下的话，直接发往stage退出地图;如果还在传送中的话，还要发往指定的stageManager和即将进入的stage里让其离开地图;

发起进入地图，进行创建
        加这个，是怕退出玩家失败;这个怎么办;对对对无所谓，我记得可以上线的时候,
        也调用进入地图，然后也判断是否在上一个地图，也会走退出上一个地图的;
        至于传送中的数据的话，确实有点难办，可以将状态也放TransferContext,
        然后弄过期时间，或者不管 每次都直接绕一圈;

        离开地图，反正也需要进行return的;

        假设将传送中的也要退出的话，应该是去oldScene，然后去Transfer的manager，然后去currScene;
        或者其实再发起一次退出地图;统一都走Manager, 然后有两个就先去old,再去curr,
        正常情况下 状态如果是在进入地图中 应该是不能发起的; 如果是状态过期了,
        过期了直接就不管，正常不会过期的，如果这次发起过期了，下一次大概率还是会过期的;
        TransferContext{ // 保存在PlayerService 的RoleMapData 数据库里面
                    long startMill; // 用来检测是否过期;
                    boolean start = false;
                   MapInfo  oldMapInfo{
                    long sceneId;
                    int mapCfgId;
                    long groupId; // 地图groupId;用来区分不同的功能，后面还可以加新字段;先用groupId这个;
                   }

                   MapInfo  targetInfo;
        }
        MapInfo currMapInfo; // 保存在PlayerService 的RoleMapData 数据库里面

        先正常进入地图，检测TransferContext是否存在，存在就return， 打warn日志;
        如果是login的话，等待2秒后直接清空，打error日志，
        然后创建新的TransferContext，  将currMapInfo放到oldMapInfo，将当前要传输的放到targetInfo，sceneId没有就设置为空就好了;
        然后发送去当前要进入的地图的Manager 使用send, Manager里先检测地图是否存在，使用 MapKey{
             int mapCfgId, long groupId;
        }
        存在的话，获取到 SMSceneInfo{
            MapKey mapKey;
            State {创建中， 已经创建， 已经销毁}
            List<playerId 以及入参数据> waitEnterQueue;
            CallPoint stageCallPoint;
        }

        如果为空，就创建SMSceneInfo 并将其设置为创建中;并且将其加入waitEnterQueue; 负载均衡stage, 然后发向指定的stage,call调用，进行创建SceneBattle(场景父类);创建完以后将其修改为已经创建,
            并且建waitEnterQueue里的玩家全部发给stage进行进入地图;
        如果是创建中，将其加入waitEnterQueue;return;
        如果已经创建，就将其发往指定的CallPoint进行加入预加入队列;
                SMSceneInfo记录在XXXDeal里，Deal里有一个Map，
                然后SceneManagerService,里有一个地方管理Deal，Map<MapType，XXXDeal>;
                Deal有个共同的父类，将基础逻辑都放里面;
        发往Stage以后，根据MapKey找到对应的sceneId,如果没有SceneId的话，就return,就是进入失败了;
        找到SceneBattle以后调用其 canEnter，可以进入的话，就进行加入pendingRoleMap队列;
        然后发给oldScene进行退出老scene地图，使用call，退出完毕以后，就回来发送消息给客户端，让客户端进入该新地图;

        然后就等客户端进度条加载完毕后发送ReadyEnterMap,发送以后就调用 SceneBattle 真正进入地图;




两个node之间断线的话，如果有一个node感知到断线，另外一个node没有感知到断线，这个时候，新链接连上去的时候，会把旧的链接断开，然后那个node并没有感知，并没有发生断线和在线事件
会造成 感知到断线那边的那个node 执行了. Service下线和上线 但是另外一边的node却只是替换链接   比如Player和场景之间player感知到断线退出地图了，但是场景没有感知到断线，没有让该玩家退出地图
    location断连接了 怎么办这个需要考虑下;


我想了下 就算是客户端确认 还是得加锁   不过我可以先加锁 解锁  然后把加锁的代码注释掉   解锁是在玩家正式进去地图以后
我这只需要在玩家正式进入地图以后 将StageActorAddress给location. Player. Online 注册地址就行了   然后就是需要注意的是 像一些队伍变化啥的需要经过Player 这样才能线性







