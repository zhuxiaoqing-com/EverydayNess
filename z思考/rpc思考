RPC用两条连接，我突然发现一个问题，比如我调用A()RPC全量同步，然后B()里调用了一个方法，发送了一个消息给A，然后B()RPC返回，
结果是BRPC先到了A，然后B()方法里发给A的后到，这样逻辑不是会有问题吗

我先调用了A，然后调用B，然后调用C，然后C再调用回B，如果需要保持顺序就让调用的时候返回一个值，就好了，不然默认就不保证顺序，你说是把


A->B  B->A
如果没有心跳，
那A服务器
    A->B 发给B，发现发不通，一种是一直重发，会很久才检测到连接不通;
    B->A 不通就一直收不到数据，永远也不会断开;

那B服务器
    A->B 不通就一直收不到数据，永远也不会断开;
    B->A 发给A，发现发不通，一种是一直重发，会很久才检测到连接不通;


客户端方，是知道自己要连接的remote是谁的，服务器方不知道;然后有个两个连接的管理;
A和B两个服务器
A服务器里的PeerLink
    RemoteNodeHandler的onChannelActive触发以后，直接调用PeerLink,
    然后发送CallNodeServicesSync标注初始化，给B，
    B收到以后，进行查看A的RemoteNode主动进行连接，B服务器的ARemoteNode触发onChannelActive后，发送CallNodeServicesSync标注初始化，给A,
    A收到以后再次调用PeerLink，A的PeerLink就调用好了，B的PeerLink其实也调用好了;
    两边的channel都放在RemoteNode里，或者放在PeerLink里,

    然后就是两边的心跳，两边都发ping,pong超时没回就调用.close,然后PeerLink在XXNodeHandler.onChannelInactive触发，我这理的是不是差不多了，

上面这种的RPC模型应该是，RPC哪个连接发的RPC，return就从哪个连接回，然后两个连接的心跳是单独的，
还有一种就是两个连接当一条连接用，就是一个发，一个回，然后ping,pong是一个连接发，一个连接回，
    只要其中一个连接不行，那ping,pong就会出问题，会回消息的时候，加上回的挂了，就给发的连接发送特殊标识，让他知道服务器挂了，
    你看哪个模型好;



大佬们 你们游戏服务器分布式，不同服务器之间的连接应该用的是两条连接吧，就两个服务器各自启动一个监听端口，然后互为客户端进行连接，进行RPC交互，这样的话就不需要关心两个服务器谁连谁的问题了，
但是有了两个连接以后，就不好做监听对方服务器上线或者下线的事件了，比如像我有一个活动服务器和场景服务器，怪物死亡以后如果某个活动是开启的，怪物掉落就翻倍，
像这个应该就是要场景服务器监听活动服务器，然后拉取全量的活动数据，活动服务器定时广播给所有的场景服务器活动数据的吧

这不是etcd干的活吗


再来个服务器做这个etcd的活嘛，所有的服务器上线以后必须先向etcd注册，然后RPC的话，两个连接默认就是不会只断一条的，所有的服务器和etcd这个服务器做连接，
将ip和端口全部注册到etcd，上线和下线的状态全部由etcd处理，只要上线了或者下线了，就触发事件，比如场景和活动服务器，场景收到活动服务器上线，直接就进行全量拉取，
活动服务器的话默认每次都向所有存活的场景服务器广播活动数据,

其实我上了etcd以后 就不关心RPC什么就断了一条之类的了，再模仿ET，只要链接创建，就先把数据缓存起来，创建完毕以后再遍历发出去，这个时候其实好像状态就完美了，
我可以直接用adminService来做etcd的事情，跨服另说，跨服的话，应该要启动一个GAdminService 只保证本服和跨服相连接，跨服之间不相互连接，你看怎么样

如果RPC真的没发出去，这个这个时候，就报错就好了，可以选择超时重发，或者直接不管，你感觉哪个好，正常不会出现这个问题，出现了怎么办 额
感觉可以超时等待重发，或者除非etcd的连接断了，不然就不管一直注册重发，对面服务器加一个幂等性就行了。如果etcd的断了，直接rpc全部失败，然后不准连;

最终决定还是用单链接，比大小的方式来做，还是要一条单链接;

RPC
总结起来有这几种
1.两条连接互相连，这种方式不能有明确的服务器上线下线回调,服务器启动的时候就连接上
2.用个nats做中转，所有服务器都连接这个，rpc消息经过nats，除了nats，其他服务器都是客户端
3.固定角色连接，就是哪个服务器是客户端，哪个服务器是服务器固定死;
4.服务发现+懒连接，懒连接就是第一次发送RPC的时候才建立连接，先把要发送的消息缓存起来，然后等链接上以后遍历发送，ET是懒连接的;
5.就是都是平等的服务器，但是用服务器id比较大小，大的或者小的当客户端连接，这样的好处和固定角色连接一样，服务器上线下线可以有明确的回调，可以做一些服务器缓存以及一致性,监听服务器上线或者下线;


将rpc改成懒加载双链接+注册中心实现node上下线;实际看service关心哪些，才连接哪些;

在org.evd.game.runtime.CallTransport.send的时候，如果是同一个node就直接调用org.evd.game.runtime.Node.handleInboundCall,
注册进去，如果不是同一个Node,就根据是否是callReturn，来进行检测session要往出口发 还是入口发，
然后这RemoteNode的两个session都写一起好吗，这个我也不清楚，感觉remoteNode里应该存的是出口的session，Node里应该弄个像ChannelManager一样，
存一个入口的session;

RPC的超时给我拆出来了，ET的Net是通过，
除了NetFiber是发送和接受，本来就两个队列，
NetFiber的话是，两个Session,主动发送方虽然也有Session，但是不会按照ip地址额外再存一份session;
但是两个Session接收到消息回调都有统一的回调;

我们这里回调也有统一的回调;存的话 目前是两个存存起来了，但是其实出口的感觉确实可以不存;其实相当于也是存的，

RPC可靠性的设计
    发送rpcId;
    收到回包的rpcId;
    每次发送RPC的时候都将这两个写进入;
    对面根据幂等性和缓存的回包数据,进行将对方已经收到的数据给删除，玛德 那这不就是KCP吗，直接用KCP不行吗 操
    其实也不是，KCP的话还有拥塞控制，乱序控制，各种东西;

     RPC单链接，由RPC链接对服务器进行上线下线逻辑，和
     RPC用两个连接，然后懒加载创建，服务器上线下线额外走连接注册中心进行长下线。
     哪个好，感觉第二个要好点，但是第二个RPC链接断开了，不等于服务器和注册中心断链了，所以没法保证消息不丢失，本来连接断开服务器下线以后，
     再次上线某些数据会全量通知一份，然后下线以后也会进行一些玩家下线处理。
     所以还是单链接好一点;









