下面的断线：指的是这些Service和自己断链后自己的处理, 上线同理
比如
A
    断线:B，C，D
指的是A检测到B,C,D和自己断线后，A服务器的处理

这里的离线和上线恢复最好全部用send,不要有call回调;
    比如Location，我可以在该服务器连接上其他service的时候，去获取其他service当前缓存的location,但是这是有问题的;
        比如我Location获取playerService的，Loc->(在这里插入了新的玩家进入player,)player->loc 这个时候这个新的玩家进入player发送了send，其实是比call回调早的，
        call回调回来就会覆盖掉旧数据覆盖新数据;好像这样子也没问题诶;按流程来说，call获取到的也是原子的数据，中间插入了新的话，那call也会获取新的，就是会重复覆盖一次;
        那就先不管了，还是可以call;但是其实也有问题;我call的时候 对面的真的已经加入到service了吗;
        但是用send，感觉更标准一点 我改一下;

哪些服务器需要在断线后处理的
AdminService    不需要处理
ConnService
    断线：
        和LocationService断开连接;不管，向LocationService发送消息的时候会报错;
        OnlineService将该gw上的人全部踢掉;直接本地踢掉;
        PlayerService将该gw上属于PlayerService的全部踢掉;会回调给online;
    上线：
        location,发送自己的actorAddress重新注册给location;

LobbyService
LocationService
    断线：
        和GW断线清理GW的;
        和Player断线清理Player的;
        和Stage断线清理Stage的;
        和Online不需要处理，因为上面离开的时候会将location的数据清理的;
    上线：

MatchService
    断线：
        和Player断线清理属于player的;
        和Online断线不需要处理，因为没和player断线的话，匹配就会继续，如果online和Player也断线的话，player就会执行下线吧匹配状态清理掉;

OnlineService
    断线：
        和gw断线，踢掉所有属于gw的玩家,会回调给playerService;
        和PlayerService断线，踢掉所有属于Player的玩家;发给GW踢;
        PlayerService的缓存状态，等n分钟后清理(UserIdPlayerServiceMap 已经实现了);这个缓存是用来处理玩家n分钟内上线后复用的;
        清理playerService和ConnService的负载均衡数据
        清理tokenMap和排队数据;

    上线：
        获取PlayerService当前Mdb的玩家在线状态;用来恢复PlayerService缓存(UserIdPlayerServiceMap 已经实现了); 废弃！！！

PlayerService
    断线：
        和gw断线，踢掉所有属于gw的玩家, 同时通知 OnlineService 删除在线状态。OnlineService 如果不拥有玩家在线状态，就不需要参与。
        和OnlineService断线，踢掉所有的玩家;本地踢掉
        和MatchService 默认取消所有在该匹配服匹配的玩家;
        和StageService断线，将所有的玩家都离开所属于该StageService的地图;(如果没有玩家所在地图数据的话 就存一下，应该是在在线玩家数据里有的)然后离开地图，
            默认进入主城;默认主城所在的StageService也不在的话，那就直接踢下线;
    上线：
        location,发送自己的actorAddress重新注册给location;
        获取PlayerService当前Mdb的玩家在线状态;用来恢复PlayerService缓存(UserIdPlayerServiceMap),发给Online; online直接清空历史的处于该playerService的数据，然后将新的数据覆盖进去;

SceneManagerService
    断线： stage断线，清理属于该stage的数据;
    上线：当Stage连上以后，获取其当前存活的Stage数据，重新初始化到内存里;

StageService
    断线：
        PlayerService将其属于Player的玩家都离开地图;
        OnlineService将其所有的场景玩家数据全部下线;(这里可以不管，毕竟Online和stage断线,online那里也没相应的处理; )
        GW将其属于GW的玩家都离开地图;(这需要吗？怎么感觉不需要 只要管player的就好了吧;gw的掉线 是不是应该player正式退出地图会管的，不然的话，退出地图感觉有些东西没有通知到啊)
                其实我感觉应该是不需要的，毕竟Stage只对StageManager和PlayerService负责吧，其他服务器的话都是间接负责
         上线：
                location,发送自己的actorAddress重新注册给location;

TeamService

RankService
    后续会做成，每个排行榜都会在相应的自己的服务器有缓存，也会存到数据库,然后定期合并上报给某个服务器;



还有个问题 现在进入地图，离开地图，都是经过SceneManager的，那就是说进入地图和离开地图会出问题;
Mdb的缓存问题;
    应该是要有一个地方有过期时间的吧;简单点，正式上线



24.延迟离线 和延迟上线，比如onServiceConnectReady，看起来是线性的,但是其实并不是,
    比如我node虽然在这个Ready的时候才进行的加入serviceList,但是我的这个onReady消息在service运行的时候，
    可能已经有其他service提前享受到了; 比如
        t1:消息AB加入service
        t2:node ready，onReady加入service
        t3:消息A轮到了service处理,因为已经ready准备完成了，但是其实OnReady还在消息AB后面，还没执行到;
    我认为是需要，每一个自己的service自己维护其他service上线下线,这样的话才能保证消息线性;而且onReady最好是，由对方service确认以后再进行的;
        就需要一个检测，如果对方service迟迟没有确认上线，怎么办？直接再次断开该node，不然没法弄;

    实现方式：
        传统的游戏服务器的话，应该也是有一个专门的线程处理的，连接上，离线，变化，都通知该线程，保证线性;
        Node收到service以后，给本地的所有service发onConnectServices(),然后准备完毕后，将onReady发给对面的service;
        怎么判断是否是同一个service呢？可以对面连上的时候，就将service里的channelId
        既然有service上线，和service下线，那是不是在上线之前不能主动发送？那可以被动发送吗？

        node连接上，
        注册service->用post回调给service;
        service收到以后，进行onReady,先发消息给service,然后通知service自己已经准备好了;
        新增启动的service也会...
        有问题，回调的话，现在是按照node来的，只要是node新增的service就会回调给其他service,
        但是新增的service自己不会受到回调的;
        正常是不是应该新增的service自己会将当前已有的所有service都触发回调一次;
        然后这个新增的需不需要保存在service里，还是说等onReady以后再保存就好了;service主动发送什么都要从自己的onReady里进行获取发送;

        然后上面的说法的话，离线以后回调到service就直接删除就好了;那个node里的离线超时回调应该没啥用;

        我是为了解决游戏运行中，服务器挂掉了，需要快速启动，快速恢复，如果没有明确的初始化数据交换完毕后，才算是准备完毕，可能会出现初始化数据还没到，这边已经有其他数据给他污染了，
        比如 错误的恢复流程
          1.StageManager 重启 内存中的场景数据丢失。 场景注册表为空
          2.Stage-1 连接成功 开始向 StageManager 上报自己持有的场景，但数据还没到达。
          3.其他服务器请求创建 sceneId=100  StageManager 查询本地注册表，发现不存在，于是分配给 Stage-2。
          4.Stage-1 的初始化数据到达 Stage-1 sceneId=100  Stage-2 sceneId=100
            此时已经出现了两个相同的场景。

          还有一种
            1.stage向stageManger同步场景数据。
            2.stage有新的过期的场景数据。
            3.等ack协议返回，才算数初始化完成。这个时候新的过期场景数据就有丢失了

        如果按我这么说 感觉是不是应该是由stagemanager来callget 所有的数据，然后确定初始化数据都get完了 才能进行通知其他所有服务器，我已经准备好了，但是这个也不太可能呀;
        感觉我有点死循环了;  因为service 肯定是陆陆续续链接过来的;

        其实我应该管的是某一个service在我这是否已经初始化完毕了
            我是stageManager
                stage连接上了，stage给stagemanger发送场景数据，发完stagemaanger就可以确定stage，其已经onReady了
            我是stage
                stageManager连接上了，stageManager直接给stage发我已经初始化完毕了，因为我本来就没啥初始化数据要给stage;

            player和loc
                我是player将哪些玩家在player服务器的数据发给loc,然后发送我已经准备完毕;
                我是loc loc直接发送已经准备完毕;这里有问题,需要等Player的数据发送完毕才能是算准备完毕;

            这里就有问题了，一个是等数据发送完成再算准备好，一个是等数据收到才算准备好，那其实最好的还是弄个n秒？
            其实时间到了，准备好，直接按照再抛给每个service让其存储下是不是就行了;

            说弄个初始化协议好像也行?先发送初始化，给每个新的service,每个新的service都确定可以了就，发送初始化完成;
            完成以后就将service加入自己的service;
                这样的好处就是不需要等待5秒;
            数据，应该是在ack里带的，还是说在发送ready里带的

        5秒一次的坏处就是，我不清楚我在你那边是否已经准备好了;我只能知道我在我这边准备好了;
        req-ack的话，是我请求你是否准备好了，你准备好了就发ack;ack回来我就把他加入准备好集合里;
        ack的话，我发送ack的时候只是确定我在你那边准备好了，他发ack回来的时候告诉我，他那边准备好了;
        这个也解决不了我的
               1.stage向stageManger同步场景数据。
               2.stage有新的过期的场景数据。
               3.等ack协议返回，才算数初始化完成。这个时候新的过期场景数据就有丢失了
         问题,
         因为我stage跟着ack回stageManager，只是stage在stageManager那边准备好了，
         但是stageManger还没在我stage这边准备好，我这场景过期了 还是同步不过去呀;

        5秒，好像还有个好处就是，可以有缓冲时间？
        就改成连接上了，就向目标服务器注册，然后注册以后跟着消息过去，
        一个是注册以后跟着消息过去;  什么消息可以跟着过去？？？
        一个是收到注册以后跟着消息过去; 接收方，自己可以发送一些需要增量推送的数据过去;
    有问题其实，我注册到对方那了，但是对方还没注册过来，会造成单向连接不通;
        要么也用node那一套，比service大小，谁小谁主动注册，然后给返回注册成功;
        这个时候就是一起注册的了;

        不能发协议注册，因为node 之间的注册消息，本来就不是同时发生的;-可以发的，因为是单链接;你能发过去，他就能回;就算以后不是单链接也没事，rpc的话正常回包都是在自己的链接上回的;

    player-online
        1.online->player 这个时候就可以进行分配player了;
        2.player->online 同时将player的mdb数据发送过去;
        如果1先, 2后就会出问题;online已经可以分配player了，但是Player的数据还没有同步过去;
    要么就是req-ackreq-ack这样;
        然后消息在req ackreq之间同步;


  最终决定保留准备状态，然后离线延迟也不删;
  增加service级别的其他service缓存，map<callpoint,OtherServiceInfo{RegisterService, boolean initDataSync 初始化数据是否同步}>;
   默认到达准备状态以后，抛给service,然后service将其加入map, service离线的时候直接将map里的数据删了;
   然后map会在service里 每秒检测，获取当前时间-service的连上时候>30秒，就进行打error日志，说明数据还没准备好;日志1分钟打印一次就行;

   逻辑其他服务器在我这到达准备状态以后，我就发个消息通知那个服务器，先将需要同步的数据发给对面的服务器，
   然后通知对面的服务器你在我这已经准备好了，对面服务器受到以后将InitDataSync设置为true;

   现在给RegisterService加了org.evd.game.runtime.ymlconfig.RegisteredService.channelId，每次对端同步node过来的时候都会将channelId，赋值;
   对端可以通过这个来检测是否正确数据;


    onConnect  需要通知老的新的上线，新的话，除了自己其他都要触发一次
    onDisconnect 只需要通知老的 新的 掉线了就行;
    onReady 目前只做了通知老的加了新的，没通知新的老的onReady; 这里问题很大;只有新加的才有5秒onReady,
            比如newNode AB     oldNode DEF

            C上线了， AB  DEF 都会收到 onConnect，然后5秒以后会收到onReady;
            但是C本身不会受到 ABDEFd的onConnect, 也不会在5秒后受到onReady;
            onConnect 其实还好，调用下就好了， 但是onReady就不一样了;
            老的是没有准备状态的，因为这个准备状态是在Node层面遍历的，还有一种就是放到service层面遍历，只要新加进去的就都有service;


org.evd.game.runtime.Node.type2ServiceMap 和 org.evd.game.runtime.Node.type2CallMap
应该都是要移动到service里去，然后每个service 都只通过自己的这两个参数来获取可以通行的service;
感觉可以在org.evd.game.runtime.OtherServiceRegistry.checkServiceConnectReady 里构建，然后缓存也可以放到
OtherServiceRegistry里，不过这个OtherServiceRegistry应该要改个更加通用的名字了吧

Service 初始化数据分为两类
1. 本地数据
    在 init 阶段初始化，包括本地配置、数据库及第三方工具中的数据。
2. 跨 Service 同步数据
需要从其他 Service 获取全量数据，存在两种情况：
    增量同步： 对方发送全量数据后，必须立即开始增量同步，避免数据丢失。
    访问控制： 对方发送全量数据后，本 Service 才能开放对目标 Service 的访问。
最复杂的情况是两者同时存在：既要保证全量与增量同步的连续性，又要在全量同步完成后才能开放访问。
    因此，只需要针对这两种情况明确同步时机和访问条件，不需要额外设置固定的 5 秒准备时间。

发送消息： 我收到并完成对方的全量数据同步后，才允许向对方发送业务消息。
接收消息： 不需要额外限制，由对方控制发送时机。对方完成我的全量数据同步后，自然就可以向我发送消息。

跨 Service 同步数据的两种情况

    1. 增量同步（由数据提供方控制）
        对方完成向我的全量数据同步后，即可开始发送增量消息，将我加入可发送消息列表。
    2. 访问控制（由数据接收方控制）
        我完成对方的全量数据同步后，才能开放依赖该数据的业务功能。
        这里不能简单地限制双方的消息收发，因为触发业务的可能是第三方 Service。
        例如：OnlineService 需要先同步 PlayerService 的负载数据，才能处理其他服务器发来的玩家登录分配请求。
        因此，访问控制必须在具体业务逻辑中判断，而不是统一限制 Service 的消息收发。

    最终方案：增量同步由数据提供方控制发送时机；业务访问由数据接收方根据自身数据是否就绪来判断。无需固定等待 5 秒。



